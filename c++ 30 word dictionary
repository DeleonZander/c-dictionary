#include <iostream>
#include <string>
using namespace std;
int main() {
    string search;
    cout << "Search? :";
    cin >> search;
    if (search == "AGING"){
        cout << "The reduction of insulation life due to thermal, voltage and other stress.";
    }
    else if(search == "AIR GAP"){
        cout << "Short gap in the magnetic material forming a magnetic.";
    }
    else if(search == "BRUSH"){
        cout << "A sliding electrical contact, usually rotating.";
    }
    else if (search == "CAPACITANCE"){
        cout << "The amount of electrical energy stored for a given electrical potential.";
    }
    else if(search == "COIL"){
        cout << "Set of series of connected turns.";
    }
    else if(search == "DATA BLOCK"){
        cout << "A sequence of data in bits or bytes that are usually transferred as a whole.";
    }
    else if(search == "DIGITAL"){
        cout << "A system in which all the information and signals are represented by binary numbers or discrete values.";
    }
    else if(search == "EARPHONE"){
        cout << "A small piece of equipment that converts electrical waves into soundwaves.";
    }
    else if(search == "ELECTROMETER"){
        cout << "A voltage measuring equipment that measures the electrical potential difference between two charged bodies without drawing any current from the source.";
    }
    else if(search == "FAILURE"){
        cout << "The malfunctioning of a device or a component that does not achieve desired target.";
    }
    else if(search == "GAMMA"){
        cout << "A unit of magnetic intensity equal to 10-5 oersted.";
    }
    else if(search == "HALT"){
        cout << "A command that is used to stop operation either permanently or temporarily.";
    }
    else if(search == "IMPLOSION"){
        cout << "The sudden inward collapse.";
    }
    else if(search == "JACK SCREW"){
        cout << "A screw-operated jack for lifting, exerting pressure or adjusting position.";
    }
    else if(search == "KEYBOARD"){
        cout << "A peripheral device that is used as data input for a computer.";
    }
    else if(search == "LADDER"){
        cout << "A circuit structure using components connected in a chain.";
    }
    else if(search == "MASKING"){
        cout << "A process in which only required data is retained and the rest is blocked, it can be using bitwise operators.";
    }
    else if(search == "NEGATE"){
        cout << "In Boolean algebra, Negate means complements.";
    }
    else if(search == "OBSOLETE"){
        cout << "These are products which are no longer manufactured or discontinued due to replacement of advanced function.";
    }
    else if(search == "PART"){
        cout << "A single component/device or a permanent assemble of more than one part that is intended to be part of somethine else.";
    }
    else if(search == "QUINT"){
        cout << "A sequence of five individual circuit blocks within the same IC package.";
    }
    else if(search == "RADIAN"){
        cout << "A unit of angulare measurement that is equal to the length that corresponds to the unit circle. 1rad = 57.3 degress.";
    }
    else if(search == "SATELLITE"){
        cout << "An artificial celestial body that is placed in orbit to round the planet in order to collect information or to establish a better communication network.";
    }
    else if(search == "TAP"){
        cout << "A fixed intermediate point in an electrical from which connections can be made.";
    }
    else if(search == "UNIT"){
        cout << "A single device or a group of devices that can operate itself and can also be assembled together to execute more complex actions.";
    }
    else if(search == "VACUUM"){
        cout << "An encolsed area of space that is relatively empty of matter.";
    }
    else if(search == "WAVE"){
        cout << "A pattern of disturbance generated by the movement of energy propagating through a medium from one location to another.";
    }
    else if(search == "X-AXIS"){
        cout << "A horizontal reference axis in a two dimensional coordinate system/graph.";
    }
    else if(search == "Y-AXIS"){
        cout << "A vertical reference axis in a two dimensional coordinate system/graph.";
    }
    else if(search == "ZERO POTENTIAL"){
        cout << "The potential across the ground terminal in any electrical circuit.";
    }
    else{ cout << "Invalid input";
    
    }
    return 0;
